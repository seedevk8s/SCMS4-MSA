spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scms2_dev?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update  # create-drop → update로 변경 (데이터 보존)
    show-sql: true
