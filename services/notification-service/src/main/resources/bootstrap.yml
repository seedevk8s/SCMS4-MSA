spring:
  application:
    name: notification-service

  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false  # Config Server 없이도 실행 가능
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.1
